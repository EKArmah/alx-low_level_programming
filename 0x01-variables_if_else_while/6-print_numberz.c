#include <stdio.h>

/**
 * main - entry point
 * @void: empty
 *
 * Return: 0 on success
 */

int main(void)
{
	int alpha;

	for (alpha = 48; alpha < 58; alpha++)
	{
		putchar(alpha);
	}
	putchar(10);

	return (0);
}
